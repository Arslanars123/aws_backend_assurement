# MongoDB Configuration
MONGODB_BASE_URI=mongodb+srv://testusername:testuserpassword@cluster0.nfgli.mongodb.net/construction_db?retryWrites=true&w=majority&appName=Cluster0

# Alternative MongoDB URI (if the above doesn't work)
# MONGODB_BASE_URI=mongodb://localhost:27017/construction_db

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_should_be_very_long_and_secure

# Email Configuration (for password reset)
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Frontend URL (for password reset links)
FRONTEND_URL=192.168.0.102:3000

# Server Configuration
PORT=3000
NODE_ENV=development 